/**
 * Configurations for PURL-Workers.
 */

/** @type {import('./src/types').Config} */
const config = {};

/** @type {import('./src/types').Entries} */
const entries = {};

export { config, entries };
